apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-k8s-app
  namespace: default

spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-k8s-app
  template:
    metadata:
      labels:
        app: node-k8s-app
    spec:
      containers:
        - name: node-app
          image: ameni1010/node-k8s-app:a6d70bb95a975329ab38ae1086accaaada8f7d8d
          imagePullPolicy: Never     # 👈 This is the key
          ports:
            - containerPort: 80
