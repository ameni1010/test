apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-k8s-app
  namespace: default

spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-k8s-app
  template:
    metadata:
      labels:
        app: node-k8s-app
    spec:
      containers:
        - name: node-app
          image: ameni1010/node-k8s-app:ffa31acc95f3e817a0cf00db37c46795758e4008
          imagePullPolicy: Never     # 👈 This is the key
          ports:
            - containerPort: 80
