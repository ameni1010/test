apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-k8s-app
  namespace: default

spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-k8s-app
  template:
    metadata:
      labels:
        app: node-k8s-app
    spec:
      containers:
        - name: node-app
          image: ameni1010/node-k8s-app:9fc607477f9013c11fd57c302b342f91cf9c00fe
          imagePullPolicy: Never     # 👈 This is the key
          ports:
            - containerPort: 80
