apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-k8s-app
  namespace: default

spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-k8s-app
  template:
    metadata:
      labels:
        app: node-k8s-app
    spec:
      containers:
        - name: node-app
          image: ameni1010/node-k8s-app:4efad747eb790e67ee267af26d756ef30e5f424a
          imagePullPolicy: Never     # 👈 This is the key
          ports:
            - containerPort: 80
